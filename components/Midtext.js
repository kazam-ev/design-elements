const Midtext = (props) => {
    return (
        <div>
            <div>
                <h2 className="resources">{props.text}</h2>
            </div>
        </div>
    );
}

export default Midtext;